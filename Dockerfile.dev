# 開発環境用Dockerfile
FROM node:22-alpine

WORKDIR /app

# 依存関係をコピーしてインストール
COPY package.json package-lock.json ./
RUN npm ci

# アプリケーションコードをコピー
COPY . .

EXPOSE 3000

# 開発サーバー起動スクリプト（Prisma生成とマイグレーションも含む）
CMD ["sh", "-c", "npx prisma generate && npx prisma migrate deploy && npm run dev"]
